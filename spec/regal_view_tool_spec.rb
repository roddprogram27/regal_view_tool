require "spec_helper"

describe RegalViewTool do
  it "has a version number" do
    expect(RegalViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
